services:
  scope-finder:
    build: .
    container_name: scope-finder
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=${SECRET_KEY}
      - DB_PATH=/app/data/scope_finder.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
